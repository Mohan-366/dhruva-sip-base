spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: ashwg-167
            name: dhruva-dynamic-config-map
      reload:
        enabled: true
        mode: event
        strategy: refresh
management:
  endpoint:
    restart:
      enabled: true
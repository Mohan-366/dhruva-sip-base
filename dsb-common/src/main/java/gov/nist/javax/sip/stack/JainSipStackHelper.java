package gov.nist.javax.sip.stack;

/**
 * The purpose of this class is to live in package "gov.nist.javax.sip.stack" and be able to access
 * the protected members of SipStackImpl.
 */
public class JainSipStackHelper {
  // TODO DSB
  //    public static void init(SIPTransactionStack sipStack, SipProperties sipProperties,
  // TrustManager trustManager, KeyManager keyManager) throws GeneralSecurityException, IOException
  // {
  //        if (sipStack.securityManagerProvider instanceof L2SipSecurityManagerProvider) {
  //            ((L2SipSecurityManagerProvider)sipStack.securityManagerProvider).init(trustManager,
  // keyManager);
  //        }
  //    }
}

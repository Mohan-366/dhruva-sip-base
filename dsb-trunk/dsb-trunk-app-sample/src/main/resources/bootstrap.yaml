spring:
  application:
    name: dsb-trunk-ft
  cloud:
    consul:
      enabled: false
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true
management:
  metrics:
    export:
      influx:
        enabled: false